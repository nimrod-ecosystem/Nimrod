# Module: <name>

> One line: what this module does and who it's for.

## Purpose
What problem it solves for a patient or caregiver. Why it exists.

## How it looks / behaves
What the user sees and does. Keep the accessibility notes here (input methods it supports:
touch, switch, scan, gaze, voice, hand/color tracking).

## Inputs → outputs (on the bus)
- **Sources:** what feeds it (keys, buttons, sensors, other modules).
- **Bindings:** the logic that turns inputs into actions.
- **Sinks:** what it writes/controls (screen, store, a device, another module).

## State & storage
What it remembers, and where. Reminder: per-user state is **server-side**, keyed to the
account — never per-device. No patient data leaves the machine.

## Privacy notes
Does it touch private/clinical data? If yes: local + open-source models only, and say so
explicitly here.

## How to extend
The most likely next changes, and where to make them.

## Status
Live / tested / planned, and any known issues.
