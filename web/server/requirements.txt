# Nimrod platform server — thin coordination layer only: accounts (stub), per-user
# state. No media, no AI.
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Postgres (deploy only — SQLite is used for local dev when DATABASE_URL is unset).
# psycopg[binary] ships a prebuilt driver; psycopg-pool is the connection pool.
psycopg[binary]>=3.2,<4
psycopg-pool>=3.2,<4

# Google login (OAuth): Authlib drives the flow, httpx is its async client,
# itsdangerous signs the session cookie (Starlette SessionMiddleware).
authlib>=1.3,<2
httpx>=0.27,<1
itsdangerous>=2,<3
